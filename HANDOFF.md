# Genki 2.0 — Handoff

## ⛔ НИКОГА НЕ ИЗПЪЛНЯВАЙ `switch-site.sh`

**`switch-site.sh` е ОТМЕНЕН (deprecated / CANCELLED). Не се пуска при никакви обстоятелства.**

- **Противоречи на текущото production заключване.** Coming Soon се управлява от `functions/_middleware.js` на `main`. Скриптът не знае за middleware-а и се бие с него.
- **Променя и разваля живия `index.html`.** Копира `index-coming-soon.html` върху него и вкарва `window.location.href` редиректи в другите страници.
- **На macOS ще се изпълни.** Писан е за macOS `sed -i ''`. На старата Windows/WSL машина мълчеше; на MacBook ще свърши работата си докрай.

**Coming Soon се включва и изключва само през текущата production архитектура** — променливата `COMING_SOON` на Pages проекта (`0` изключва гейта). Никога чрез редакция на файлове и никога чрез този скрипт.

Файлът остава в репото и правата му не се променят — нарочно решение. Не го изтривай, не му сваляй `+x`, не го пускай.

## Къде сме
**Етап 6 — Контакт: ЗАВЪРШЕН (2026-09-20).** Етапи 1–6 са готови и одобрени.

**Следващ: Етап 7 — Genki Fit. НЕ Е ЗАПОЧНАТ и не се започва.** Чака отделна business спецификация от собственика за точно два отворени въпроса:

- **Q3** — динамичните диапазони за дневна посещаемост спрямо размера на офиса от Q2.
- **Q6** — динамичните месечни бюджетни граници в € спрямо контекста на компанията и избраното Benefit / Product Price Support / и двете.

Това са **бизнес решения**. Не се измислят, не се избират стойности самостоятелно и не се запълва празнината мълчаливо. Брифът раздел 15 казва само „Dynamic ranges based on Q2" и „Use dynamic € bands" — без числа. `v2/genki-fit.html` остава чист скелет до одобрението.

> **Asset ID схемата е заключена:** каноничната VISUAL ASSET MAP печели. ID носят само реални производствени активи; компонентите не консумират ID. Регистърът е в `docs/GENKI-2.0-VISUAL-ASSET-PRODUCTION-PLAN.md`. Чака одобрение единствено G07 за логата на производителите.

> Окончателна геометрия на активите има само Home. За останалите страници тя е provisional и се уточнява след build-а на съответния етап. MAIKOZ върви паралелно и не блокира build-а.

Целият план, чеклистите по етапи и подробният отчет за етап 1 са в `docs/BUILD-PLAN.md`. Това е файлът, от който се разбира докъде сме стигнали.

## Клон
Работи се в `genki-2.0-build`. `main` остава непокътнат и обслужва живия сайт до cut-over.

## Production състояние (към 2026-09-22)

**`genki.bg` е ЗАКЛЮЧЕН зад Coming Soon страница.** Genki 2.0 **не е публичен**.

- Гейтът е `functions/_middleware.js` — Pages Function, която живее на **`main`**, не на този клон. `/` сервира `coming-soon.html` на място; всяка друга страница е 302 към `/`.
- **Не се гейтват:** `/api/*`, `/box` и `/box/<КОД>/` (живата QR система за партньорски кутии), правните страници, и всичко, което не е HTML.
- **Защита на preview-то:** Cloudflare Access пази preview deployment-ите. Отделно `PREVIEW_TOKEN` на Pages проекта отваря пълния сайт през `https://www.genki.bg/?preview=<стойността>` — слага бисквитка за 7 дни.
- **Изключване на гейта:** `COMING_SOON=0` на Pages проекта. Не чрез редакция на файлове.
- **Box/QR системата остава жива** и не бива да се чупи.

Middleware-ът и Coming Soon инфраструктурата **не се пипат от този клон.**

### Променливи на Pages проекта (Production)

| | |
| --- | --- |
| `RESEND_API_KEY` | encrypted secret. **Конфигуриран и работещ.** Без него формите връщат `not_configured` и НЕ твърдят успех |
| `GENKI_RATE` | KV binding за ограничение на честотата — **вързан и реализиран** в `functions/api/contact.js` |
| `GENKI_SCANS` | KV binding за броене на сканиранията |
| `PREVIEW_TOKEN` | преглед на пълния сайт въпреки гейта |
| `COMING_SOON` | `0` изключва гейта |

Променливите влизат в сила при следващия deployment, не веднага.

## Deploy — истината

**Production е GitHub-connected.** Cloudflare Pages следи `main`:

> **`git push origin main` МОЖЕ ДА ПРЕДИЗВИКА PRODUCTION DEPLOYMENT.**

Push към `main` не е „синхронизация на репото" — това е публикуване.

- **Wrangler НЕ е нормалният път за deploy.** Ползва се само когато собственикът изрично го поиска за конкретния случай.
- **Наличието на `CLOUDFLARE_API_TOKEN` на машината НЕ е разрешение за deploy.** На този MacBook токенът е зададен; това значи само, че командата технически би минала.
- Push към `genki-2.0-build` не пуска production deployment.
- **Няма автоматичен публичен launch.** Cut-over-ът от Coming Soon става само след личен преглед и изрично одобрение.

## Какво е готово
- **Design tokens** — `css/genki-tokens.css`. Брандовото зелено е `#1d5329` (измерено от логото), акцентът `#218336`. `#2d8659` е отменено. Скалата е калибрирана срещу `docs/DESIGN-DIRECTION.md` и проверена на 8 ширини.
- **12-колонен grid** (12 / 8 / 4), редакционна типографска скала с роли, нова радиус система, motion примитиви.
- **Без Tailwind** — 407 KB JS по-малко на страница. Стиловете са чист CSS върху токени.
- **Home е изградена** до production ниво в `v2/index.html` — пет секции, дословното copy от бриф раздел 7 и 8, editorial split hero.
- **За компании е изградена** до production ниво в `v2/companies.html` — шест секции, дословното BG copy от бриф раздел 9.
- **Как работи Genki е изградена** до production ниво в `v2/how-it-works.html` — шест секции, BG и EN дословно от каноничния документ.
- **Мисия и въздействие е изградена** до production ниво в `v2/mission.html` — шест секции, BG и EN дословно от каноничния документ.
- **Контакт е изградена и работеща** — `v2/contact.html` + `functions/api/contact.js` (Pages Function → Resend → hello@genki.bg).
- **Скелет** за Genki Fit в `v2/`.
- **План за производство на визуалните активи** — `docs/GENKI-2.0-VISUAL-ASSET-PRODUCTION-PLAN.md`.
- **Хедър, мобилно меню, BG/EN превключвател, футър** — един източник в `tools/partials/`, синхронизира се с `node tools/sync-partials.js`.
- **VisualSlot** `<genki-slot slot-id="H01">` — точните съотношения, alt на двата езика, `<picture>` API за отделни desktop/mobile кадри, override на съотношението за композиция.

## Как се тества локално
```
python -m http.server 8765
```
после `http://localhost:8765/v2/index.html`

След промяна в хедъра или футъра:
```
node tools/sync-partials.js          # преписва партиалите в 6-те страници
node tools/sync-partials.js --check  # проверява без да пише
```

## Каноничен източник за copy

**„GENKI 2.0 — WEBSITE COPY & HANDOFF"** (Google Doc, LOCKED 16.09.2026, ID `19GINMhMzUKR8rv3dS-b3TpvUEP9C_seA8ZdXKNthu5g`) е каноничният източник за текста на сайта — BG и EN. Съдържа пълна заключена EN версия за всички страници. **Не се превежда на ръка.**

## Имейл — състояние

**Работи на production.** Resend доставя до `hello@genki.bg`.

- `RESEND_API_KEY` е **конфигуриран** като encrypted secret на Pages проекта. (По-ранният запис, че „трябва да се добави" и че е secret само на стария Worker, е **отменен** — вече не е верен.)
- **Реална доставка е тествана успешно** — и за записването от Coming Soon (`functions/api/subscribe.js`, на `main`), и за формата за контакт (`functions/api/contact.js`). (По-ранният запис „реален имейл тест още не е правен" е **отменен**.)
- **Rate limiting е реализиран** — `GENKI_RATE` KV binding, кодът е в `functions/api/contact.js`. Ако KV не е вързан, лимитът просто не се прилага; функцията не се чупи.
- Без ключ функцията връща `not_configured` и **не твърди успех** — нарочно, за да няма фалшив success state.
- Всички човешки времена в имейлите са `Europe/Sofia` през `lib/genki-time.js`.

## ⚠ ОТВОРЕНО — ротация на стария Resend ключ

**Това НЕ е затворено само защото доставката работи.** Работещата доставка доказва, че *новият* ключ е валиден. Тя не доказва, че *старият* е отнет.

Какво доказва репото:

- `RESEND-SETUP.md` е бил комитнат с истински ключ в `d644fc6`, изтрит е в `a3a72be` — но **низът остава достъпен в историята на публичното репо** (проверено с `git log -S`, 2026-09-22).
- **Няма доказателство в репото, че старият ключ е ротиран или отнет.** Ротацията се случва в Resend dashboard-а, не в git — репото по принцип не може да я докаже.

Остава като отворена задача:

> **Провери и довърши ротацията на стария Resend API ключ.** Потвърди в Resend dashboard-а, че компрометираният ключ от `d644fc6` е revoked и че нито едно живо изпращане не минава през него. Чак тогава този item се затваря — и се затваря с изрично потвърждение от собственика, не по косвени признаци.

Отделно решение, което още не е взето: дали историята на публичното репо да се изчисти от низа. Не се прави самостоятелно — това е пренаписване на история.

## Часова зона — заключено правило

**Бизнес часовата зона на целия Genki ecosystem е `Europe/Sofia`.** Глобално правило, важи и за всяка бъдеща функционалност, включително целия Genki Fit. Каноничният модул е `lib/genki-time.js`; пълната спецификация е в `CLAUDE.md` раздел 7.1.

Накратко: машинното съхранение може да е UTC ISO; всичко, което човек вижда, и всяка граница на ден са софийски. Никога зашито отместване, никога часовата зона на устройството, никога суров ISO низ пред човек.

Тестове: `node tools/dev/test-time.mjs` (36) и `node tools/dev/test-qr-time.mjs` (16) — покриват зима, лято и двата DST прехода.

## Отворени въпроси
- **Q3 и Q6 на Genki Fit** — динамичните диапазони за посещаемост и бюджетните граници в €. Блокират етап 7. Бизнес решение на собственика, не се измислят.
- **Ротацията на стария Resend ключ** — виж отделната секция по-горе.
- Векторен файл на логото — няма и не се чака скоро. G01/G02/G03 стоят на PNG с максимална резолюция и са маркирани „чака векторен файл".
- Липсва страница „Cookies", която бриф раздел 25 изброява във футъра. Не е сложен мъртъв линк.
- Писмени разрешения от седемте производителя за логата; логото на Rawlls липсва изцяло.
- Шестте хардуерни слота чакат MAIKOZ или първата реална машина.
- Снимки на хардуера от MAIKOZ, разрешения от производителите за логата, спецификация на Genki Fit — виж таблицата със зависимости в `docs/BUILD-PLAN.md`.

## Какво НЕ е пипано
Legacy сайтът в root (`index.html`, `companies.html`, `mission.html`, `contact.html`, `office.html`, `privacy*.html`, `404.html`, `box-landing.html`), старите `js/main.js`, `js/translations.js`, `css/custom.css`, и цялата box/QR система (`box/`, `functions/api/qr.js`). Всичко това продължава да работи.
